# Setup — Run these on the VM terminal

Use your sudo password when prompted.

## 1. Install Claude Code for claude-worker
sudo npm install -g @anthropic-ai/claude-code

## 2. Login to Claude as the isolated user
sudo -u claude-worker claude login

---

After step 2, message me on Telegram and I'll take it from here.
